import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.craftsanddigitals.app',
  appName: 'schedule-tracker',
  webDir: 'www'
};

export default config;
